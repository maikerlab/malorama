---
title: "Introduction to MDX"
date: "January 11, 2023"
description: Create your own Blog using Next.js and the power of MDX files
image: "javascript.png"
tags: ["blog", "mdx"]
---

<div>
  This is a blog post about <strong>MDX</strong>. <br />
  We'll learn what MDX is and how to use it for creating a blog.
</div>

<br />

### Headings

Just use "hashtags" (e. g. `###`) to add (sub-)headings in your blog post like in normal markdown files.

### Syntax Highlighting

We can show code snippets by using the `SyntaxHighlighter` component.

With `language="javascript"`:

<div>
  <CodeHighlight
    language="javascript"
    codeString={`
    const doStuff = () => {
      console.log('hey');
    }
  `}
  />
</div>

With `language="kotlin"`:

<CodeHighlight
  language="kotlin"
  codeString={`
    fun getEvent(id: Int) {
      val event = await http.get("localhost:8080/event/$id")
      when(event.day) {
          "Monday" -> Toast.makeText(this, "The event is on Monday!", Toast.LENGTH_SHORT).show()
          else -> { finish() }
      }
    }
  `}
/>

With `language="kotlin"` and with optional caption: `caption="Rust example"`:

<CodeHighlight
  language="rust"
  codeString={`
  fn main() {
    println!("Hello, world!");
  }
  `}
  caption="Rust example"
/>

We can also use simple code blocks by including code between 3 quotation marks.

```
  // this is a code block
  const doSomething = () => {
    return console.log('hey')
  }
```

### Using React components

I have created a component named "Expandable", where I used a [Disclosure element from Headless UI](https://headlessui.com/react/disclosure) and styled it to fit my needs.
Now I can use it inside my MDX files like this:

`<Expandable title="Hello" content="World" />`

<Expandable title="Click to open me!" content="Hello, World!" />
<Expandable
  title="How does it look with a longer text inside?"
  content="Lorem ipsum dolor sit amet consectetur adipisicing elit. Explicabo delectus ad quam magnam distinctio provident reprehenderit possimus odit aliquam adipisci. Dolores molestiae culpa autem, obcaecati esse error, inventore possimus pariatur quis, temporibus doloremque quos dolor. Modi optio veniam possimus, facilis libero in at explicabo nihil nostrum. Voluptas ad eaque reiciendis id, error veniam quo, dicta libero doloribus neque nobis nesciunt sequi! Debitis dolore in rem. Provident earum ab aspernatur praesentium cumque laboriosam, reprehenderit porro neque ipsum, nam sint omnis modi, dolorem consequatur a commodi similique totam eius. Animi odit laudantium id magnam, optio in error architecto. Officia perspiciatis cumque laudantium!"
/>
